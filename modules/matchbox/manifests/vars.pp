class matchbox::vars {
    $app_url           = lookup("matchbox_app_url")
    $feed_url          = lookup("matchbox_feed_url")
    $home_dir          = lookup("generic_home_dir")
    $preferred_browser = lookup("preferred_browser")
    $runtime_user      = lookup("matchbox_runtime_user")
}
