class slim::vars {
    $autologin    = lookup("slim_auto_login")
    $conf_dir     = lookup("slim_conf_dir")
    $console_cmd  = lookup("slim_console_cmd")
    $login_cmd    = lookup("slim_login_cmd")
    $run_dir      = lookup("slim_run_dir")
    $runtime_user = lookup("generic_user")
    $server_args  = lookup("slim_server_args")
    $suspend_cmd  = lookup("slim_suspend_cmd")
    $theme        = lookup("slim_theme")
    $xauth        = lookup("xorg_xauth")
    $xbin         = lookup("xorg_bin")
}
