class nitrogen::vars {
    $home_dir      = lookup("generic_home_dir")
    $posx          = lookup("nitrogen_posx")
    $posy          = lookup("nitrogen_posy")
    $runtime_group = lookup("generic_group")
    $runtime_user  = lookup("generic_user")
    $sizex         = lookup("nitrogen_sizex")
    $sizey         = lookup("nitrogen_sizey")
}
