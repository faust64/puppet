class newznab::vars {
    $contact           = lookup("newznab_contact")
    $ldap_object_login = lookup("newznab_ldap_object_login")
    $ldap_object_mail  = lookup("newznab_ldap_object_mail")
    $ldap_slave        = lookup("openldap_ldap_slave")
    $ldap_user_base    = lookup("newznab_ldap_user_base")
    $mysql_db          = lookup("newznab_mysql_database")
    $mysql_pass        = lookup("newznab_mysql_password")
    $mysql_user        = lookup("newznab_mysql_username")
    $nntp_host         = lookup("newznab_nntp_host")
    $nntp_pass         = lookup("newznab_nntp_password")
    $nntp_user         = lookup("newznab_nntp_username")
    $rdomain           = lookup("root_domain")
    $runtime_user      = lookup("apache_runtime_user")
    $slack_hook        = lookup("newznab_slack_hook_uri")
    $svn_password      = lookup("newznab_svn_password")
    $svn_username      = lookup("newznab_svn_username")
    $web_root          = lookup("apache_web_root")
    $with_cache        = lookup("newznab_memcache_backend")
}
