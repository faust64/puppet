class newznab::vars {
    $contact           = hiera("newznab_contact")
    $ldap_object_login = hiera("newznab_ldap_object_login")
    $ldap_object_mail  = hiera("newznab_ldap_object_mail")
    $ldap_slave        = hiera("openldap_ldap_slave")
    $ldap_user_base    = hiera("newznab_ldap_user_base")
    $mysql_db          = hiera("newznab_mysql_database")
    $mysql_pass        = hiera("newznab_mysql_password")
    $mysql_user        = hiera("newznab_mysql_username")
    $nntp_host         = hiera("newznab_nntp_host")
    $nntp_pass         = hiera("newznab_nntp_password")
    $nntp_user         = hiera("newznab_nntp_username")
    $rdomain           = hiera("root_domain")
    $runtime_user      = hiera("apache_runtime_user")
    $slack_hook        = hiera("newznab_slack_hook_uri")
    $svn_password      = hiera("newznab_svn_password")
    $svn_username      = hiera("newznab_svn_username")
    $web_root          = hiera("apache_web_root")
    $with_cache        = hiera("newznab_memcache_backend")
}
