class thruk::vars {
    $admin_contact   = hiera("thruk_admin_contact")
    $apache_conf_dir = hiera("apache_conf_dir")
    $apache_srvname  = hiera("apache_service_name")
    $backends        = hiera("thruk_backends")
    $base_dir        = hiera("thruk_base_dir")
    $conf_dir        = hiera("thruk_conf_dir")
    $download        = hiera("download_cmd")
    $home_link       = hiera("thruk_home_link")
    $listen_ports    = hiera("apache_listen_ports")
    $plugins         = hiera("thruk_plugins")
    $plugins_drop    = hiera("thruk_drop_plugins")
    $rdomain         = hiera("root_domain")
    $repo            = hiera("puppet_http_repo")
    $runtime_group   = hiera("apache_runtime_group")
    $runtime_user    = hiera("apache_runtime_user")
    $start_timeout   = hiera("thruk_start_timeout")
    $with_icinga     = hiera("thruk_with_icinga")
    $with_shinken    = hiera("thruk_with_shinken")
}
