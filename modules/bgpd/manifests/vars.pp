class bgpd::vars {
    $bgp_auth            = hiera("bgp_auth")
    $bgp_allow_networks  = hiera("bgp_allow_networks")
    $bgp_as_no           = hiera("bgp_as_no")
    $bgp_conf_dir        = hiera("bgp_conf_dir")
    $bgp_database        = hiera("bgp_database")
    $bgp_deny_networks   = hiera("bgp_deny_networks")
    $bgp_holdtime        = hiera("bgp_holdtime")
    $bgp_log_updates     = hiera("bgp_log_updates")
    $bgp_map             = hiera("bgp_map")
    $bgp_max_pfx_len     = hiera("bgp_max_pfx_len")
    $bgp_min_pfx_len     = hiera("bgp_min_pfx_len")
    $bgp_pass            = hiera("bgp_pass")
    $bgp_redistribute    = hiera("bgp_redistribute")
    $bgp_route_collector = hiera("bgp_route_collector")
    $bgp_router_id       = hiera("bgp_router_id")
    $bgp_service_name    = hiera("bgp_service_name")
    $gre_tunnels         = hiera("gre_tunnels")
    $nagios_runtime_user = hiera("nagios_runtime_user")
    $sudo_conf_dir       = hiera("sudo_conf_dir")
}
