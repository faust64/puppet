class bgpd::vars {
    $bgp_auth            = lookup("bgp_auth")
    $bgp_allow_networks  = lookup("bgp_allow_networks")
    $bgp_as_no           = lookup("bgp_as_no")
    $bgp_conf_dir        = lookup("bgp_conf_dir")
    $bgp_database        = lookup("bgp_database")
    $bgp_deny_networks   = lookup("bgp_deny_networks")
    $bgp_holdtime        = lookup("bgp_holdtime")
    $bgp_log_updates     = lookup("bgp_log_updates")
    $bgp_map             = lookup("bgp_map")
    $bgp_max_pfx_len     = lookup("bgp_max_pfx_len")
    $bgp_min_pfx_len     = lookup("bgp_min_pfx_len")
    $bgp_pass            = lookup("bgp_pass")
    $bgp_redistribute    = lookup("bgp_redistribute")
    $bgp_route_collector = lookup("bgp_route_collector")
    $bgp_router_id       = lookup("bgp_router_id")
    $bgp_service_name    = lookup("bgp_service_name")
    $gre_tunnels         = lookup("gre_tunnels")
    $nagios_runtime_user = lookup("nagios_runtime_user")
    $sudo_conf_dir       = lookup("sudo_conf_dir")
}
