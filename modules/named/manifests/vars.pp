class named::vars {
    $all_networks       = hiera_hash("vlan_database")
    $conf_dir           = hiera("named_conf_dir")
    $contact            = hiera("named_contact")
    $dhcp_ip            = hiera("dhcp_ip")
    $do_domains         = hiera("named_gen_domains")
    $is_datacenter      = hiera("is_datacenter")
    $ldap_slave         = hiera("openldap_ldap_slave")
    $local_networks     = hiera("active_vlans")
    $log_dir            = hiera("named_log_dir")
    $munin_conf_dir     = hiera("munin_conf_dir")
    $munin_monitored    = hiera("named_munin")
    $munin_probes       = hiera("named_munin_probes")
    $munin_service_name = hiera("munin_node_service_name")
    $named_channels     = hiera("named_channels")
    $named_notify       = hiera("named_notify")
    $named_master       = hiera("named_master")
    $office_netids      = hiera("office_netids")
    $rndc_key           = hiera("named_rndc_key")
    $runtime_conf_dir   = hiera("named_runtime_conf_dir")
    $runtime_group      = hiera("named_runtime_group")
    $runtime_log_dir    = hiera("named_runtime_log_dir")
    $runtime_user       = hiera("named_runtime_user")
    $runtime_zone_dir   = hiera("named_runtime_zone_dir")
    $service_name       = hiera("named_service_name")
    $sshfp_domains      = hiera("named_sshfp_domains")
    $with_collectd      = hiera("named_collectd")
    $zone_dir           = hiera("named_zone_dir")
    $zones_check        = hiera("named_zones_check")
}
