class ossec::vars {
    $accept_new_agents    = hiera("ossec_accept_new_agents")
    $app_directories      = hiera("ossec_app_directories")
    $cache_ip             = hiera("squid_ip")
    $check_directories    = hiera("ossec_check_directories")
    $conf_dir             = hiera("ossec_conf_dir")
    $contact              = hiera("ossec_contact")
    $do_rsyslog           = hiera("rsyslog_to_ossec")
    $download             = hiera("download_cmd")
    $frequency            = hiera("ossec_check_frequency")
    $ignore               = hiera("ossec_ignore")
    $mail_from            = hiera("ossec_mail_from")
    $mail_relay           = hiera("mail_mx")
    $manager              = hiera("ossec_manager")
    $rdomain              = hiera("root_domain")
    $realtime             = hiera("ossec_realtime")
    $report_changes       = hiera("ossec_report_changes")
    $rsyslog_conf_dir     = hiera("rsyslog_conf_dir")
    $rsyslog_service_name = hiera("rsyslog_service_name")
    $rsyslog_version      = hiera("rsyslog_version")
    $rules                = hiera("ossec_include_rules")
    $service_name         = hiera("ossec_service_name")
    $slack_hook           = hiera("ossec_slack_hook_uri")
    $timezone             = hiera("locale_tz")
    $whitelist            = hiera("ossec_whitelist")
}
