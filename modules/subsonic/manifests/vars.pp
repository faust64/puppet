class subsonic::vars {
    $alerts               = lookup("subsonic_alerts")
    $do_flac              = lookup("subsonic_do_flac")
    $getting_started      = lookup("subsonic_getting_started")
    $ldap_base            = lookup("subsonic_searchuser")
    $ldap_slave           = lookup("openldap_ldap_slave")
    $listen_ports         = lookup("apache_listen_ports")
    $locale               = lookup("locale")
    $max_mem              = lookup("subsonic_allocate")
    $message              = lookup("subsonic_message")
    $maintitle            = lookup("subsonic_title")
    $music_root           = lookup("subsonic_music_root")
    $slack_hook           = lookup("subsonic_slack_hook_uri")
    $rdomain              = lookup("root_domain")
    $rsyslog_conf_dir     = lookup("rsyslog_conf_dir")
    $rsyslog_service_name = lookup("rsyslog_service_name")
    $ssh_port             = lookup("ssh_port")
    $subtitle             = lookup("subsonic_subtitle")
    $sync_bwlimit         = lookup("subsonic_sync_bwlimit")
    $sync_directories     = lookup("subsonic_sync_directories")
}
