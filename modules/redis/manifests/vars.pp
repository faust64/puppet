class redis::vars {
    $conf_dir         = hiera("redis_conf_dir")
    $databases        = hiera("redis_databases")
    $down_timeout     = hiera("redis_sentinel_down_timeout")
    $failover_timeout = hiera("redis_sentinel_failover_timeout")
    $limits           = hiera("redis_output_buffer_limits")
    $listen_address   = hiera("redis_listen_address")
    $listen_port      = hiera("redis_listen_port")
    $sentinel_name    = hiera("redis_sentinel_name")
    $sentinel_port    = hiera("redis_sentinel_port")
    $sentinel_quorum  = hiera("redis_sentinel_quorum")
    $munin_monitored  = hiera("redis_munin")
    $munin_probes     = hiera("redis_munin_probes")
    $repl_backlog     = hiera("redis_repl_backlog_size")
    $repl_pass        = hiera("redis_repl_pass")
    $runtime_group    = hiera("redis_runtime_group")
    $runtime_user     = hiera("redis_runtime_user")
    $save             = hiera("redis_save")
    $service_name     = hiera("redis_service_name")
    $serve_stale      = hiera("redis_serve_stale_data")
    $slave_priority   = hiera("redis_slave_priority")
    $slave_read_only  = hiera("redis_slave_read_only")
    $slavelag         = hiera("redis_slave_max_lag")
    $slavemin         = hiera("redis_min_slave_to_write")
    $slaveof          = hiera("redis_slaveof")
    $tcp_keepalive    = hiera("redis_tcp_keepalive")
    $timeout          = hiera("redis_timeout")
}
