class transmission::vars {
    $conf_dir           = hiera("transmission_conf_dir")
    $contact            = hiera("transmission_contact")
    $contact_escalate   = hiera("nagios_contact_escalate")
    $dht_enable         = hiera("transmission_dht_enable")
    $forwarding         = hiera("transmission_forwarding")
    $lib_dir            = hiera("transmission_lib_dir")
    $munin_conf_dir     = hiera("munin_conf_dir")
    $munin_monitored    = hiera("transmission_munin")
    $munin_probes       = hiera("transmission_munin_probes")
    $munin_service_name = hiera("munin_node_service_name")
    $peer_port_default  = hiera("transmission_peer_port_default")
    $peer_port_high     = hiera("transmission_peer_port_high")
    $peer_port_low      = hiera("transmission_peer_port_low")
    $rdomain            = hiera("root_domain")
    $rpc_dismiss_auth   = hiera("transmission_rpc_skip_auth")
    $rpc_listen         = hiera("transmission_rpc_listen")
    $rpc_passphrase     = hiera("transmission_rpc_passphrase")
    $runtime_group      = hiera("transmission_runtime_group")
    $runtime_user       = hiera("transmission_runtime_user")
    $srvname            = hiera("transmission_service_name")
    $store_dir          = hiera("transmission_store_dir")
}
