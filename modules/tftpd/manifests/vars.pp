class tftpd::vars {
    $apt_proxy    = hiera("apt_cacher")
    $charset      = hiera("locale_charset")
    $clear_pass   = hiera("ui_host_clear_password")
    $contact      = hiera("contact_alerts")
    $default_pass = hiera("ks_host_hashed_password")
    $download     = hiera("download_cmd")
    $dns_ip       = hiera("dns_ip")
    $locale       = hiera("locale")
    $locale_long  = hiera("locale_LOCALE")
    $ntp_upstream = hiera("ntp_upstream")
    $obsd_nics    = hiera("ui_openbsd_nics")
    $preseed_pass = hiera("ps_host_hashed_password")
    $puppetmaster = hiera("puppetmaster")
    $pxe_master   = hiera("pxe_reference_repository")
    $pxe_packages = hiera("pxe_packages_repository")
    $repo         = hiera("puppet_http_repo")
    $root_dir     = hiera("pxe_root_dir")
    $runtime_user = hiera("tftp_runtime_user")
    $squid_ip     = hiera("squid_ip")
    $ssh_keys     = hiera("ssh_keys_database")
    $timezone     = hiera("locale_tz")
    $wds_ip       = hiera("wds_ip")
}
