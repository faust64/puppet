class xen::vars {
    $apt_proxy          = hiera("apt_cacher")
    $default_vg         = hiera("virtual_default_vg")
    $dns_ip             = hiera("dns_ip")
    $has_openvz         = hiera("openvz_and_xen")
    $jumeau             = hiera("jumeau")
    $mail_hub           = hiera("mail_mx")
    $mem_max            = hiera("xen_mem_max")
    $mem_reserved       = hiera("xen_mem_reserved")
    $modeles_nas        = hiera("modeles_nas")
    $modeles_fsopts     = hiera("modeles_nfs_opts")
    $modeles_path       = hiera("modeles_path")
    $munin_conf_dir     = hiera("munin_conf_dir")
    $munin_monitored    = hiera("xen_munin")
    $munin_probes       = hiera("xen_munin_probes")
    $munin_service_name = hiera("munin_node_service_name")
    $repo               = hiera("puppet_http_repo")
    $vz_default_bridge  = hiera("vz_default_bridge")
    $vz_default_gateway = hiera("vz_default_gateway")
    $vz_default_netmask = hiera("vz_default_netmask")
    $with_collectd      = hiera("xen_collectd")
}
