server {
    listen 127.0.0.1:<%=@listen_ports['plain']%>;
    server_name <%=@name%>;
    server_tokens off;
    location /nginx_status {
	stub_status on;
	access_log off;
	allow 127.0.0.1;
	deny all;
    }
}
